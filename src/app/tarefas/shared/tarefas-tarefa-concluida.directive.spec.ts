import { TarefasTarefaConcluidaDirective } from './tarefas-tarefa-concluida.directive';

describe('TarefasTarefaConcluidaDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new TarefasTarefaConcluidaDirective();
  //   expect(directive).toBeTruthy();
  // });
});
